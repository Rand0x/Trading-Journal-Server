# Docker network binding. Use 0.0.0.0 only for trusted local-network devices.
HOST_BIND_ADDRESS=127.0.0.1

# Optional comma-separated browser origins for a separate frontend.
ALLOWED_ORIGINS=

# Optional HTTP Basic Auth for the web interface and management API.
# Set both values or leave both empty.
JOURNAL_USERNAME=
JOURNAL_PASSWORD=
